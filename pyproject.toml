[project]
name = "bot-factory"
version = "0.1.4"
description = "Core for the creating discord bot, which is based on Disnake"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cryptography>=46.0.6",
    "disnake>=2.12.0",
    "json5>=0.14.0",
    "loguru>=0.7.3",
    "pydantic>=2.12.5",
    "rich>=14.3.3",
]
