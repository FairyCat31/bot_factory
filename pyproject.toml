[project]
name = "bot-factory"
version = "0.1.2"
description = "Core for the creating discord bot, which is based on Disnake"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "colorama>=0.4.6",
    "cryptography>=46.0.5",
    "disnake>=2.12.0",
    "json5>=0.13.0",
]
